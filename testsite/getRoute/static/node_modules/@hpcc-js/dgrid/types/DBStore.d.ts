import { Database } from "@hpcc-js/common";
import { CellFormatter, CellRenderer, ColumnType } from "./RowFormatter";
export declare class DBStore {
    private _db;
    Model: null;
    idProperty: "__hpcc_id";
    constructor(db: Database.Grid);
    db2Columns(sortable: boolean, fields: Database.Field[], prefix?: string, formatter?: CellFormatter, renderCell?: CellRenderer): ColumnType[];
    columns(sortable: boolean, formatter?: CellFormatter, renderCell?: CellRenderer): ColumnType[];
    getIdentity(object: any): any;
    _fetchRange(opts: {
        start: number;
        end: number;
    }): object[];
    fetchRange(opts: {
        start: number;
        end: number;
    }): Promise<object[]>;
    sort(opts: any): this;
}
//# sourceMappingURL=DBStore.d.ts.map